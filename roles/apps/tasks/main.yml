---
- name: Install applications
  community.general.pacman:
    name:
      - firefox
      - chromium
      - discord
      - thunderbird
      - gimp
      - inkscape
      - drawio-desktop
      - libreoffice-fresh
      - obsidian
      - vlc
      - ffmpeg          # CLI media toolkit; backs the `normalize-audio` script (dotfiles bin/)
    state: present